# MongoDB 連線字串
MONGODB_URL=mongodb://localhost:27017
# JWT 簽章密鑰，請用：python -c "import secrets;print(secrets.token_hex(32))"
SECRET_KEY=change_me_to_a_random_64_char_hex
# 允許的前端來源（逗號分隔），上線時填正式網域
ALLOWED_ORIGINS=http://localhost:5173
